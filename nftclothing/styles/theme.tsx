import { extendTheme } from '@chakra-ui/react'

const theme = extendTheme({
  fonts: {
    h1: `'Cinzel Variable', sans-serif`,
    h2: `'Abril Fatface', sans-serif`,
  },
})

export default theme